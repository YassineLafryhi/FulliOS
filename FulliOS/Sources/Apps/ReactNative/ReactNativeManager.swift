//
//  ReactNativeManager.swift
//  FulliOS
//
//  Created by Yassine Lafryhi on 25/7/2024.
//

import Foundation
// import React

internal class ReactNativeManager: NSObject {
    static let shared = ReactNativeManager()

    // TODO: Fix this

    /* lazy var bridge: RCTBridge = {
         let jsCodeLocation = RCTBundleURLProvider.sharedSettings().jsBundleURL(forBundleRoot: "index", fallbackResource: nil)
         return RCTBridge(bundleURL: jsCodeLocation, moduleProvider: nil, launchOptions: nil)
     }()

     func viewForModule(_ moduleName: String) -> RCTRootView {
         RCTRootView(
             bridge: bridge,
             moduleName: moduleName,
             initialProperties: nil)
     } */
}
